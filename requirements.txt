# ─────────────────────────────────────────────────────────
# Meeting-to-Task Automation System – Requirements
# ─────────────────────────────────────────────────────────
# Install everything:  pip install -r requirements.txt
# Minimal install:     pip install -r requirements-minimal.txt
# ─────────────────────────────────────────────────────────

# ── Backend API ────────────────────────────────────────────
fastapi==0.111.0          # Async Python web framework
uvicorn[standard]==0.29.0 # ASGI server (runs FastAPI)
python-multipart==0.0.9   # Required for FastAPI file uploads
pydantic==2.7.1           # Data validation
requests==2.31.0          # HTTP client (for Ollama / Gemini calls)
python-dotenv==1.0.1      # Load .env files

# ── Frontend ───────────────────────────────────────────────
streamlit==1.35.0         # UI framework

# ── Transcription (CPU Whisper) ────────────────────────────
faster-whisper==1.0.2     # CPU-optimised Whisper (uses CTranslate2)
# Note: faster-whisper will auto-download the model on first run.
# Default model: base (~74 MB download, ~370 MB RAM)
# Change WHISPER_MODEL=tiny for lower RAM usage.

# ── Utilities ──────────────────────────────────────────────
python-jose==3.3.0        # (optional) JWT if you add auth later
